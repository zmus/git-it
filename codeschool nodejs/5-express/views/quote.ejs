<!-- 
	EJS template (embedded javascript)
	Installs the module and adds to package.json: $ npm install --save ejs

	%=  -  	RENDER that variable inside html (print its content)
	%   -   just RUN THE CODE without showing it in html
-->


<h2>Quote by <%= name %></h2>

<blockquote>
  <%= quote %>
</blockquote>
